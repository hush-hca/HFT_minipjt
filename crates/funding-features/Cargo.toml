[package]
name = "funding-features"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
funding-core = { path = "../funding-core" }
md-core = { path = "../md-core" }
